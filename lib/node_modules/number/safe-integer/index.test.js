var safeInteger = require('.');
var b = require('@timelaps/batterie');
b.describe('safeInteger', function () {
    b.expect(safeInteger).toBeFunction();
    b.expect(safeInteger()).toBeUndefined();
    b.expect(safeInteger(1e100)).toBe(require('@timelaps/constants/max-safe-integer'));
});