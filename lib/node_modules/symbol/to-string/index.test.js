var symbolToString = require('.');
var b = require('batterie');
b.describe('symbol.toString', function () {
    b.expect(symbolToString)[Symbol && Symbol.prototype ? 'toBeFunction' : 'notToBeFunction']();
});