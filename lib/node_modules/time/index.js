var time = module.exports = require('time/make');
var merge = require('object/merge');
merge(time, {
    now: require('time/now'),
    zone: require('time/zone'),
    parse: require('time/parse'),
    constants: require('time/constants'),
    offset: require('time/offset'),
    semantic: require('time/semantic')
});