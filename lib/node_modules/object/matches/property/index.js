module.exports = property;
var isEqual = require('@timelaps/is/equal');
var access = require('object/access');

function property(pair) {
    var key = pair[0],
        value = pair[1];
    return function (item) {
        return isEqual(access(item, key), value);
    };
}