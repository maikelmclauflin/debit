var isUndefined = require('is/undefined');
var keys = require('object/keys');
var isStrictlyEqual = require('is/strictly-equal');
var returnsBaseType = require('returns/base-type');
module.exports = function (o1_, o2, customizer, _stack) {
    var key, i = 0,
        instanceKeys = keys(o2),
        stack = _stack || [],
        l = instanceKeys.length,
        o1 = o1_;
    if (l) {
        o1 = o1 || returnsBaseType(o2);
        for (; i < l; i++) {
            key = instanceKeys[i];
            customizer(o1[key], o2[key], key, o1, o2, stack);
        }
    }
    return o1;
};