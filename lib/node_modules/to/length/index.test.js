var toLength = require('.');
var b = require('batterie');
b.describe('to.length', function () {
    b.expect(toLength).toBeFunction();
    b.expect(toLength()).toBe(0);
    b.expect(toLength(1e14)).toBeLessThan(1e14);
    b.expect(toLength(-1)).toBe(0);
});