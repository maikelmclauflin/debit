var objectSet = require('object/set/simple');
module.exports = function (collection, bound, array) {
    return array ? function (item, index, objs) {
        collection.push(bound(item, index, objs));
    } : function (item, key, objs) {
        objectSet(collection, bound(item, key, objs), key);
    };
};