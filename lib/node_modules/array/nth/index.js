module.exports = nth;
var isStrictlyEqual = require('@timelaps/is/strictly-equal');
var toNumberCoerce = require('@timelaps/hacks/to-number');

function nth(array, index) {
    var idx = toNumberCoerce(index);
    if (array && !isStrictlyEqual(idx, -1)) {
        return array && array[idx];
    }
}