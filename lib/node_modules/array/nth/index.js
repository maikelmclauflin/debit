module.exports = nth;
var isStrictlyEqual = require('is/strictly-equal');
var toNumberCoerce = require('to/number');

function nth(array, index) {
    var idx = toNumberCoerce(index);
    if (array && !isStrictlyEqual(idx, -1)) {
        return array && array[idx];
    }
}