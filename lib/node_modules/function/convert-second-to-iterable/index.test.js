var convertSecondToIterable = require('.');
var b = require('@timelaps/batterie');
b.describe('convertSecondToIterable', function () {
    b.expect(convertSecondToIterable).toBeFunction();
    b.expect(convertSecondToIterable()).toBeFunction();
});